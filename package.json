{
  "name": "swarm-sandbox",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Minimal zero-dependency Node healthcheck sandbox.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "healthcheck": "node scripts/healthcheck.js",
    "test": "node scripts/healthcheck.js",
    "start": "node server.js",
    "test:api": "node scripts/test-api.js && node test-api.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
